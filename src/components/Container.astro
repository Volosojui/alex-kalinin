---
interface Props {
  /** Element to render as. Defaults to a plain div. */
  as?: string;
  id?: string;
  class?: string;
}

const { as: Tag = 'div', id, class: className } = Astro.props;
---

<Tag id={id} class:list={['mx-auto max-w-3xl px-6 sm:px-8', className]}>
  <slot />
</Tag>
